import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    list: {
    paddingBottom: 6,
    fontSize: 16,
    lineHeight: 21,
    fontWeight: 'bold',
    letterSpacing: 3,
    color: 'black',
    },
});

export default styles;