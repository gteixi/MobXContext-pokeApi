import { ViewStyle } from "react-native";

const AppStyles = {
  screen: {
    mainScreen: {
      flex: 1,
    },
  },
  container: {
    alignItems: "stretch",
    flex: 1,
    justifyContent: "flex-start",
  } as ViewStyle,
};

export default AppStyles;